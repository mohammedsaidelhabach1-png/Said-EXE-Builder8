# خطة تصميم واجهة Said EXE

## فكرة التطبيق

تطبيق Android عربي باسم **Said EXE** يعمل كمركز مبسط لإدارة مشاريع البناء، حزم المكتبات، إعدادات البيئة، وسجل العمليات. يركز التصميم على الاستخدام بيد واحدة، والوضوح، والانتقال السريع بين الأدوات.

## الشاشات

| الشاشة | المحتوى والوظيفة |
|---|---|
| الرئيسية | ترحيب باسم Said EXE، حالة البيئة، أزرار بدء مشروع ومشاهدة المكتبات وآخر العمليات |
| المشاريع | قائمة المشاريع المحفوظة، إنشاء مشروع، فتح مشروع، حذف أو إعادة تسمية عبر حوار تأكيد |
| تفاصيل المشروع | اسم المشروع، نوعه، المكتبات المرتبطة، حالة الإعداد، وأزرار تجهيز أو فحص |
| المكتبات | بحث وتصنيف المكتبات، تحديد عدة مكتبات بمربعات اختيار، وإضافتها للمشروع |
| البناء | اختيار المشروع، نوع الحزمة، بدء العملية، شريط تقدم، ونتيجة قابلة للمشاركة |
| الإعدادات | اسم التطبيق الظاهر، مجلد العمل، اللغة، الوضع الداكن، الإشعارات، والحفظ التلقائي |
| الحقوق | اسم Said EXE، حقوق النشر، وروابط Internet Freebies الرسمية |
| الحوار | إنشاء مشروع، اختيار صيغة، تأكيد الحذف، عرض النتيجة، ورسائل الخطأ الودية |

## التدفقات الأساسية

1. يفتح المستخدم الرئيسية ثم يضغط «مشروع جديد»، يدخل الاسم، ويؤكد من الحوار.
2. يفتح المستخدم المشروع، ينتقل إلى المكتبات، يحدد العناصر بمربعات اختيار، ثم يضغط «إضافة للمشروع».
3. يفتح المستخدم البناء، يختار نوع الحزمة، يراجع الخيارات، ثم يبدأ العملية ويرى النتيجة في حوار واضح.
4. يفتح المستخدم الإعدادات، يغيّر الخيارات، وتُحفظ التغييرات تلقائياً دون زر حفظ.
5. يفتح المستخدم الحقوق للوصول إلى القنوات والصفحات الرسمية داخل المتصفح.

## الهوية البصرية

يستخدم التطبيق خلفية كحلية عميقة `#0B1220`، وبطاقات بلون `#111827`، وأزرق سماوي رئيسي `#38BDF8`، وأخضر نجاح `#22C55E`، وبرتقالي تنبيه `#F59E0B`. العناوين كبيرة وواضحة، والأزرار الأساسية بعرض مناسب للإبهام، مع مساحات لمس لا تقل عن حجم مريح للاستخدام اليومي.
